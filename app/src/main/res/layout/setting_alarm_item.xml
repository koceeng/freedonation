<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/height_small"
    android:paddingTop="5dp"
    android:paddingBottom="5dp"
    android:paddingStart="@dimen/space_default"
    android:paddingEnd="@dimen/space_default"
    android:orientation="vertical" >

    <FrameLayout
        android:id="@+id/setting_alarm_item_button"
        android:layout_width="@dimen/height_small"
        android:layout_height="@dimen/height_small"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:foreground="?attr/selectableItemBackground">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:src="@drawable/close"
            android:tint="@color/colorDanger"
            android:contentDescription="@null"/>

    </FrameLayout>

    <TextView
        android:id="@+id/setting_alarm_item_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@id/setting_alarm_item_button"
        android:layout_gravity="center_vertical"
        android:textSize="@dimen/text_default"/>

</RelativeLayout>